---
description: Generate a conventional commit message.
agent: build
model: opencode/grok-code
---

Generate a conventional commit message on my behalf for the staged changes. See the diff and the last 5 full commit messages to match the pattern. If all the last 5 commits are merge commits then see the full commits messages of last 10 commits. Output only the final commit message block.

